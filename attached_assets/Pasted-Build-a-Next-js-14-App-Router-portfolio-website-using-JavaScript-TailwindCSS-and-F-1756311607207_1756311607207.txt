Build a **Next.js 14 (App Router)** portfolio website using **JavaScript**, **TailwindCSS**, and **Framer Motion**.

### Requirements:
- **Use my uploaded resume (PDF)** to auto-fill:
  - Name: "Yash Gautam"
  - Role: "Full-Stack & DevOps Engineer"
  - Summary from resume
  - Skills from resume
  - Projects: Docify and simple-google-oauthentication with descriptions and tech stack
  - Social links: GitHub, LinkedIn, Email from resume

---

### Pages:
1. **Home (`/`)**
   - Fullscreen hero section with:
     - Name: "Yash Gautam"
     - Subtitle: "Full-Stack & DevOps Engineer"
     - Social buttons (GitHub, LinkedIn, Email)
       - Style: Glassmorphism pill
         - Default: Transparent with neon border
         - Hover: Smooth center-out gradient fill (violet → cyan), text turns white
         - Transition: 0.4s ease, no ripple/blink
     - CTA button: "View Projects" → navigates to `/projects`
   - Disable scroll (`overflow-hidden`)
   - Animations:
     - Text: Fade-in + slide-up on page load
     - Buttons: Staggered fade + scale-in
   - Background:
     - Animated gradient waves (violet, cyan, magenta)
     - Floating neon particles that:
       - Connect with glowing lines when near each other
       - Slightly repel from cursor
     - GPU-friendly (Canvas with requestAnimationFrame)

2. **Projects (`/projects`)**
   - Scroll enabled
   - Sticky navbar:
     - Left: "← Home" link
     - Right: Site title
   - Projects grid:
     - Each card = Glassmorphism
     - Content:
       - Project title
       - Short description
       - Tech badges
       - Buttons: Visit & Code (same hover animation as home buttons)
     - Hover effect:
       - Card softly fills with neon glow from center
       - Transition 0.4s smooth
   - Animate cards with Framer Motion (staggered fade + slide-up)

---

### Global Design:
- Dark theme (#0a0a0a background)
- Neon gradient accents (violet → cyan)
- Modern font: Inter (Google Fonts)
- Fully responsive

---

### Deliverables:
- Full Next.js 14 app with:
  - `app/` directory for routes
  - `components/` for Hero, Navbar, Background, ProjectCard, Button
- TailwindCSS configured
- Framer Motion animations applied
- Background effect implemented in a reusable component
- Use the uploaded resume for content

⚡ Goal: A polished, modern, dark-themed portfolio with futuristic background and smooth animations.
