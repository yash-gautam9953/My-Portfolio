Build a **Next.js 14 (App Router)** portfolio using **JavaScript**, **TailwindCSS**, and **Framer Motion**. Use my uploaded resume to auto-fill personal info, projects, and links.

---

### ✅ Global Design:
- Dark futuristic theme with neon accents (violet → cyan).
- Glassmorphism for buttons and cards.
- Font: Inter from Google Fonts.
- Fully responsive.

---

### ✅ Pages:

1. **Home (`/`)**
   - Fullscreen hero section:
     - Name: "Yash Gautam"
     - Subtitle: "Full-Stack & DevOps Engineer"
     - Social buttons: GitHub, LinkedIn, Email
       - Style: Glassmorphism pill
       - Hover: smooth **center-out gradient fill** (violet → cyan), text turns white
     - CTA button: "View Projects" → `/projects`
   - Disable scroll (`overflow-hidden`)
   - Animations:
     - Hero text: fade-in + slide-up
     - Buttons: staggered fade + scale-in
   - **Background**:
     - Canvas-based **particle system**:
       - Particles float smoothly
       - **Connect with glowing lines when near**
       - **Cursor hover effect:** particles **repel slightly from cursor**
       - Neon glow for particles and lines
     - Subtle animated gradient waves behind particles

2. **Projects (`/projects`)**
   - Sticky navbar:
     - Left: "← Home"
     - Right: Site title
   - Grid of project cards:
     - Glassmorphism style
     - Each card:
       - Title, description, tech badges
       - Buttons (Visit, Code) with same hover effect as hero buttons
     - Hover: card background softly fills with neon glow
   - Animations:
     - Cards animate in with Framer Motion (staggered fade + slide-up)
   - **Background**:
     - Same particle animation as home page (Canvas)
     - Hover interaction same as above

---

### ✅ Deliverables:
- Full Next.js 14 app with:
  - `app/` for routes
  - `components/`:
    - `Hero.js`
    - `Navbar.js`
    - `Button.js` (with neon hover animation)
    - `ProjectCard.js`
    - `Background.js` (**Canvas-based particle system** reusable on multiple pages)
- TailwindCSS configured
- Framer Motion animations added
- Resume content (name, projects, links) populated dynamically

⚡ Goal:
Create a **modern, dark-themed, responsive portfolio** with **interactive neon particles background on both pages** and **smooth UI animations**.
